<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>You're invited</title>
</head>
<body style="margin: 0; padding: 0; font-family: system-ui, -apple-system, sans-serif; line-height: 1.5; color: #1a1a1a;">
  <div style="max-width: 560px; margin: 0 auto; padding: 24px;">
    <h1 style="margin: 0 0 16px; font-size: 24px; font-weight: 600;">You're invited</h1>
    <p style="margin: 0 0 24px;">Hi,</p>
    <p style="margin: 0 0 24px;">You've been invited to join. {{#if inviteLink}}Click the button below to accept the invite and get started.{{else}}Use the token below to accept your invite on the sign-up or accept-invite page.{{/if}}</p>
    {{#if inviteLink}}
    <p style="margin: 0 0 24px;">
      <a href="{{inviteLink}}" style="display: inline-block; padding: 12px 24px; background: #1a1a1a; color: #fff; text-decoration: none; border-radius: 6px; font-weight: 500;">Accept invite</a>
    </p>
    <p style="margin: 0 0 24px; font-size: 14px; color: #666;">Or copy this link: <a href="{{inviteLink}}" style="color: #1a1a1a; word-break: break-all;">{{inviteLink}}</a></p>
    {{else}}
    {{#if token}}
    <p style="margin: 0 0 24px; font-size: 14px; color: #666;">Your invite token (copy and use on the accept-invite page):</p>
    <p style="margin: 0 0 24px; font-family: monospace; font-size: 14px; background: #f4f4f4; padding: 12px; border-radius: 6px; word-break: break-all;">{{token}}</p>
    {{/if}}
    {{/if}}
    {{#if expiresAt}}
    <p style="margin: 0 0 24px; font-size: 14px; color: #666;">This invite expires on {{expiresAt}}.</p>
    {{/if}}
    {{#if roleName}}
    <p style="margin: 0 0 24px; font-size: 14px; color: #666;">Role: {{roleName}}</p>
    {{/if}}
    <p style="margin: 0; font-size: 14px; color: #666;">If you didn't expect this invite, you can ignore this email.</p>
  </div>
</body>
</html>
